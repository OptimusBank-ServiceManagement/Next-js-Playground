trigger:
- app-dev

pr: none

pool:
  name: Default   # your self-hosted Windows agent pool name

# Reference to your Next.js app repo (same Azure DevOps project)
resources:
  repositories:
    - repository: nextApp
      type: git
      name: YourNextJsRepo   # <-- change this to your actual repo name

variables:
  - group: smtp-variables

  - name: nodeVersion
    value: '18.x'

  - name: repoName
    value: $[replace(variables['Build.Repository.Name'], '.*/','')]
  - name: siteName
    value: '$(repoName)'
  - name: physicalPath
    value: 'C:\inetpub\wwwroot\$(repoName)'
  - name: appPoolName
    value: '$(repoName)-pool'
  - name: parentSiteName
    value: 'Optimus Apps'

# ===============================================
# 1) VALIDATE — ensure this is a Next.js project
# ===============================================
stages:
- stage: Validate
  displayName: 'Validate Project'
  jobs:
  - job: CheckProject
    displayName: 'Check for Next.js Project'
    steps:
    - checkout: self
      displayName: 'Checkout CICD Repo'
    - checkout: nextApp
      path: s/nextApp
      displayName: 'Checkout Next.js App Repo'

    - task: CmdLine@2
      displayName: 'Check Next.js marker'
      name: ProjectCheck
      inputs:
        script: |
          set APPROOT=$(Build.SourcesDirectory)\nextApp
          echo Checking for Next.js marker...
          if not exist "%APPROOT%\package.json" (
            echo No package.json found in app repo
            echo ##vso[task.setvariable variable=hasProject;isOutput=true]false
            exit /b 0
          )
          findstr /i "\"next\"" "%APPROOT%\package.json" >nul 2>nul
          if errorlevel 1 (
            echo "next" dependency not found in package.json
            echo ##vso[task.setvariable variable=hasProject;isOutput=true]false
          ) else (
            echo Next.js dependency found
            echo ##vso[task.setvariable variable=hasProject;isOutput=true]true
          )

# ===============================================
# 2) BUILD — install, lint, test, build, package
# ===============================================
- stage: Build
  displayName: 'Build Application'
  dependsOn: Validate
  condition: eq(dependencies.Validate.outputs['CheckProject.ProjectCheck.hasProject'], 'true')
  jobs:
  - job: BuildNextJs
    displayName: 'Build Next.js Application'
    steps:
    - checkout: self
      displayName: 'Checkout CICD Repo'
    - checkout: nextApp
      path: s/nextApp
      displayName: 'Checkout Next.js App Repo'

    - task: NodeTool@0
      displayName: 'Use Node $(nodeVersion)'
      inputs:
        versionSpec: '$(nodeVersion)'

    - task: CmdLine@2
      displayName: 'Log Node & npm versions'
      inputs:
        script: |
          echo ===== Node & npm Versions =====
          node -v
          npm -v
          echo ===== PATH =====
          echo %PATH%

    - task: Cache@2
      displayName: 'Cache node_modules'
      inputs:
        key: 'npm | "$(Agent.OS)" | $(Build.SourcesDirectory)/nextApp/package-lock.json'
        restoreKeys: |
          npm | "$(Agent.OS)"
        path: $(Build.SourcesDirectory)/nextApp/node_modules

    - task: CmdLine@2
      displayName: 'Install Dependencies'
      inputs:
        script: |
          cd /d "$(Build.SourcesDirectory)\nextApp"
          if exist package-lock.json (
            echo Using npm ci
            npm ci --prefer-offline
          ) else if exist yarn.lock (
            echo Using yarn install
            yarn install --frozen-lockfile --prefer-offline
          ) else (
            echo No lockfile found
            npm install
          )

    - task: CmdLine@2
      displayName: 'Run lint (non-blocking)'
      inputs:
        script: |
          cd /d "$(Build.SourcesDirectory)\nextApp"
          if exist package.json (
            npm run -s | findstr /b /i "  lint" >nul 2>nul
            if not errorlevel 1 (
              echo Running lint...
              npm run lint -- --no-fix --quiet || echo Lint completed with warnings.
            ) else (
              echo No lint script; skipping
            )
          )

    - task: CmdLine@2
      displayName: 'Run tests (if configured)'
      inputs:
        script: |
          cd /d "$(Build.SourcesDirectory)\nextApp"
          npm run -s | findstr /b /i "  test" >nul 2>nul
          if not errorlevel 1 (
            echo Running tests...
            npm run test -- --watchAll=false --ci=true --coverage --reporters=default --reporters=jest-junit || exit /b 1
          ) else (
            echo No test script found; skipping
          )

    - task: PublishTestResults@2
      displayName: 'Publish test results'
      condition: succeededOrFailed()
      inputs:
        testResultsFormat: 'JUnit'
        testResultsFiles: '**/junit.xml'
        failTaskOnFailedTests: true

    - task: PublishCodeCoverageResults@1
      displayName: 'Publish coverage'
      condition: succeededOrFailed()
      inputs:
        codeCoverageTool: 'Cobertura'
        summaryFileLocation: '$(Build.SourcesDirectory)/nextApp/coverage/cobertura-coverage.xml'

    - task: CmdLine@2
      displayName: 'Build Application'
      inputs:
        script: |
          cd /d "$(Build.SourcesDirectory)\nextApp"
          set NODE_ENV=production
          set NEXT_TELEMETRY_DISABLED=1
          set NODE_OPTIONS=--max_old_space_size=4096
          echo Building Next.js app...
          npm run build || exit /b 1
          if not exist ".next" (
            echo Build failed: .next not found
            exit /b 1
          ) else (
            echo Build succeeded.
          )

    - task: CmdLine@2
      name: ExportCheck
      displayName: 'Static export check'
      inputs:
        script: |
          cd /d "$(Build.SourcesDirectory)\nextApp"
          set USE_STATIC=false
          npm run -s | findstr /b /i "  export" >nul 2>nul
          if not errorlevel 1 (
            echo Running export...
            npm run export
            if exist "out" set USE_STATIC=true
          )
          echo ##vso[task.setvariable variable=useStaticExport;isOutput=true]%USE_STATIC%

    - task: CmdLine@2
      displayName: 'Package for deployment'
      inputs:
        script: |
          set PUBLISH=$(Build.ArtifactStagingDirectory)\publish
          set APPROOT=$(Build.SourcesDirectory)\nextApp
          set USE_STATIC=$(ExportCheck.useStaticExport)
          if not exist "%PUBLISH%" mkdir "%PUBLISH%"
          cd /d "%APPROOT%"
          if /i "%USE_STATIC%"=="true" (
            echo Copying static export...
            robocopy "out" "%PUBLISH%" /E /NFL /NDL /NJH /NJS /NC /NS
          ) else (
            echo Copying server files...
            robocopy ".next" "%PUBLISH%\.next" /E
            if exist public robocopy public "%PUBLISH%\public" /E
            if exist package.json copy /Y package.json "%PUBLISH%"
            if exist package-lock.json copy /Y package-lock.json "%PUBLISH%"
            if exist yarn.lock copy /Y yarn.lock "%PUBLISH%"
            for %%F in (next.config.js next.config.cjs next.config.mjs) do if exist %%F copy /Y %%F "%PUBLISH%"
          )
          dir "%PUBLISH%"

    - task: PublishBuildArtifacts@1
      displayName: 'Publish build artifacts'
      inputs:
        pathToPublish: '$(Build.ArtifactStagingDirectory)/publish'
        artifactName: 'next-app'
        publishLocation: 'Container'

# ===============================================
# 3) DEPLOY — copy to IIS, setup app pool, warmup
# ===============================================
- stage: Deploy
  displayName: 'Deploy to IIS'
  dependsOn:
  - Validate
  - Build
  condition: and(succeeded(), eq(dependencies.Validate.outputs['CheckProject.ProjectCheck.hasProject'], 'true'))
  variables:
    useStaticExport: $[ stageDependencies.Build.BuildNextJs.outputs['ExportCheck.useStaticExport'] ]
  jobs:
  - deployment: DeployToIIS
    displayName: 'Deploy to IIS Server'
    environment: 'Production'
    strategy:
      runOnce:
        deploy:
          steps:
          - task: DownloadBuildArtifacts@1
            displayName: 'Download Build Artifacts'
            inputs:
              buildType: 'current'
              artifactName: 'next-app'
              downloadType: 'single'
              downloadPath: '$(Pipeline.Workspace)'

          - task: CmdLine@2
            displayName: 'Deploy to IIS'
            inputs:
              script: |
                set SITENAME=$(siteName)
                set PHYSPATH=$(physicalPath)
                set APPPOOL=$(appPoolName)
                set PARENTSITE=$(parentSiteName)
                set SOURCE=$(Pipeline.Workspace)\next-app
                set USE_STATIC=$(useStaticExport)
                echo Deploying %SITENAME%
                set APPCMD=%windir%\system32\inetsrv\appcmd.exe
                if not exist "%SOURCE%" exit /b 1
                "%APPCMD%" list apppool /name:"%APPPOOL%" >nul 2>nul
                if not errorlevel 1 "%APPCMD%" stop apppool /apppool.name:"%APPPOOL%"
                "%APPCMD%" list apppool /name:"%APPPOOL%" >nul 2>nul
                if errorlevel 1 "%APPCMD%" add apppool /name:"%APPPOOL%"
                if /i "%USE_STATIC%"=="true" (
                  "%APPCMD%" set apppool "%APPPOOL%" /managedRuntimeVersion:v4.0
                ) else (
                  "%APPCMD%" set apppool "%APPPOOL%" /managedRuntimeVersion:
                )
                if exist "%PHYSPATH%" rd /s /q "%PHYSPATH%"
                mkdir "%PHYSPATH%"
                "%APPCMD%" list app "%PARENTSITE%/%SITENAME%" >nul 2>nul
                if errorlevel 1 (
                  "%APPCMD%" add app /site.name:"%PARENTSITE%" /path:/"%SITENAME%" /physicalPath:"%PHYSPATH%" /applicationPool:"%APPPOOL%"
                ) else (
                  "%APPCMD%" set vdir "%PARENTSITE%/%SITENAME%/" /physicalPath:"%PHYSPATH%"
                )
                robocopy "%SOURCE%" "%PHYSPATH%" /MIR /NFL /NDL /NJH /NJS /NC /NS
                if /i not "%USE_STATIC%"=="true" (
                  cd /d "%PHYSPATH%"
                  if exist package-lock.json npm ci --only=production --no-audit
                  if exist yarn.lock yarn install --production --frozen-lockfile
                )
                icacls "%PHYSPATH%" /grant "IIS AppPool\%APPPOOL%":(OI)(CI)F /T
                icacls "%PHYSPATH%" /grant IIS_IUSRS:(OI)(CI)RX /T
                "%APPCMD%" start apppool /apppool.name:"%APPPOOL%"

          - task: CmdLine@2
            displayName: 'Warm up application'
            inputs:
              script: |
                set URL=https://optiweb.optimusbank.com:8025/$(siteName)
                echo Warming up %URL%
                ping -n 15 127.0.0.1 >nul
                curl --insecure --max-time 30 -s -o NUL "%URL%"
                echo Warm-up completed.

# ===============================================
# 4) NOTIFY — summary log (email optional)
# ===============================================
- stage: Notify
  displayName: 'Final Summary'
  dependsOn:
  - Validate
  - Deploy
  condition: always()
  jobs:
  - job: FinalSummary
    displayName: 'Pipeline Summary'
    steps:
    - task: CmdLine@2
      displayName: 'Print summary'
      inputs:
        script: |
          echo ==========================================
          echo PIPELINE EXECUTION SUMMARY
          echo ==========================================
          echo Application : $(siteName)
          echo Status      : $(Agent.JobStatus)
          echo Build       : $(Build.BuildNumber)
          echo Branch      : $(Build.SourceBranchName)
          echo URL         : https://optiweb.optimusbank.com:8025/$(siteName)
          echo ==========================================
